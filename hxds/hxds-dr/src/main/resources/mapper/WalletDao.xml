<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hxds.dr.db.dao.WalletDao">
    <insert id="insert" parameterType="com.example.hxds.dr.db.pojo.WalletEntity">
        INSERT INTO tb_wallet
        SET driver_id=#{driverId},
            balance=#{balance},
            password=#{password}
    </insert>

    <select id="searchWalletByDriverId" parameterType="long" resultType="HashMap">
        SELECT CAST(balance AS CHAR) AS balance
        FROM tb_wallet
        WHERE driver_id = #{driverId}
    </select>
</mapper>
