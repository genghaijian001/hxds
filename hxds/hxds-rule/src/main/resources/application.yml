server:
  jetty:
    threads:
      acceptors: 4
      selectors: 8
  port: 8006
  servlet:
    context-path: /

spring:
  profiles:
    include: common
  application:
    name: hxds-rule
  cloud:
    inetutils:
      preferred-networks: 127.0.0.1
    nacos:
      #配置子系统在Web管理页面上的注册信息
      config:
        group: DEFAULT_GROUP
        file-extension: yaml
        server-addr: 127.0.0.1:8848
        prefix: ${spring.application.name}
        namespace: hxds
      #配置子系统在Nacos上的注册信息
      discovery:
        namespace: hxds
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848
        heart-beat-interval: 1000
        heart-beat-timeout: 1000
        group: DEFAULT_GROUP
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/hxds_rule?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
      username: root
      password: 123456
      initial-size: 4
      max-active: 8
      min-idle: 8
      max-wait: 60000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.example.hxds.rule.db.pojo
  configuration:
    map-underscore-to-camel-case: true


springdoc:
  api-docs:
    enabled: true
    path: /doc-api.html
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: on
seata:
  enabled: true
  application-id: ${spring.application.name}
  tx-service-group: hxds_tx_group
  service:
    vgroup-mapping:
      hxds_tx_group: default
    grouplist:
      default: 127.0.0.1:8091

feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 300000